# 开发环境 - npm run dev / Webpack 开发
# Vite 仅注入以 VITE_ 开头的变量到前端

# 环境标识：development | production | test
VITE_APP_ENV=development

# API 基础地址（开发时通常由 devServer.proxy 代理 /api，可留空表示相对路径 /api）
VITE_APP_API_BASE_URL=/api

# 开发/测试时反向代理 /api 的目标地址（Webpack devServer.proxy 与 Vite server.proxy 共用）
VITE_APP_PROXY_TARGET=http://localhost:5900

# AES 加解密密钥，与后端 AesEncryption:Key 一致（16/24/32 字节）；留空则不加密
VITE_AES_KEY=whevether_123456

# 登录后页面水印文案，留空则不显示水印
VITE_APP_WATERMARK_TEXT=开发环境
